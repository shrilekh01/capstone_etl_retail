[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "etl_core"
version = "0.1.0"
description = "Retail ETL Core Platform"
requires-python = ">=3.9"
dependencies = []
